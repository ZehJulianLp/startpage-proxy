{
  "name": "startpage-transport-proxy",
  "version": "1.0.0",
  "description": "Transport and RSS proxy for a personal startpage",
  "main": "server.js",
  "scripts": {
    "start": "PORT=56669 node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "fast-xml-parser": "^4.4.1",
    "gtfs-realtime-bindings": "^1.1.1"
  }
}
